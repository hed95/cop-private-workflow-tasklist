apiVersion: v1
kind: Service
metadata:
  name: borders-workflow-tasklist
  labels:
    name: borders-workflow-tasklist
spec:
  type: ClusterIP
  ports:
  - name: https
    port: 443
    targetPort: 10443
  selector:
    name: borders-workflow-tasklist